import React from 'react';

import { Text, View } from '../../components/Themed';

const SettingsPage = () => {
	return (
		<View className="flex flex-grow">
			<Text className=" text-center text-lg mt-5">Settings</Text>
		</View>
	);
};

export default SettingsPage;
